<h1>Articles</h1>
<hr>
<% @articles.each do |article| %>
  <p>
    <%= link_to(article.title, article.body, target: "_blank") %> - <%= link_to("Show Page", article) %>
    <%= link_to(article.title, article.body, target: "_blank") %> - <%= link_to("Show Page", article) %> | <%= link_to 'Destroy Article', article_path(article), method: :delete %> 
  </p>
<% end %>
<hr>
<%= link_to 'New Article', new_article_path, class: 'btn btn-primary' %>
<hr>
